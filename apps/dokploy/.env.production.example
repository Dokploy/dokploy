PORT=3000
NODE_ENV=production

# Optional override for Better Auth / passkeys.
# Most self-hosted installs: leave unset — Settings → Server (Host + HTTPS) is enough.
# Set only when env, not the UI, should own the public URL. See plans/passkey-auth.md.
# BETTER_AUTH_URL="https://dokploy.example.com"