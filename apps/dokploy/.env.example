DATABASE_URL="postgres://dokploy:amukds4wi9001583845717ad2@localhost:5432/dokploy"
PORT=3000
NODE_ENV=development

# Optional override for Better Auth / passkeys (scheme + host + port if non-default).
# Most installs: leave unset — use Settings → Server (Host + HTTPS) instead.
# See plans/passkey-auth.md for when to set this.
# BETTER_AUTH_URL="http://localhost:3000"
